<Page x:Class="Growth.Pages.Outlet.List"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Growth.Pages.Outlet"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DesignHeight="524" d:DesignWidth="1136"
      Title="List">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Grid Background="#EEEEEE" Height="40" Margin="0 0 0 0" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10 0 0 0">
                    <fa:ImageAwesome Icon="User" Foreground="#3A3A3A" Height="14" Width="14"/>
                    <TextBlock FontFamily="Verdana" FontSize="14" Margin="20 0 0 0">Outlet List</TextBlock>
                </StackPanel>
            </Grid>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20">
                <StackPanel Orientation="Vertical">
                    <Button HorizontalAlignment="Left" Background="#2D89EF" Foreground="White" Padding="10 5 10 5" Click="Button_Click">Tambah Outlet</Button>
                    <ListView x:Name="listOutlet" Margin="0 15 0 0" TextElement.FontSize="13" Foreground="Black">
                        <ListView.View>
                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style>
                                        <Setter Property="TextElement.FontSize" Value="15" />
                                        <Setter Property="TextElement.FontWeight" Value="Bold"/>
                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn Header="View" Width="70">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <fa:ImageAwesome Icon="Search" Height="12" Width="12" Margin="0 0 20 0" Foreground="Black"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Nama Outlet" Width="139" DisplayMemberBinding="{Binding nm_outlet}" />
                                <GridViewColumn Header="Alamat" Width="139" DisplayMemberBinding="{Binding almt_outlet}" />
                                <GridViewColumn Header="Kode Pos" Width="100" DisplayMemberBinding="{Binding kodepos}" />
                                <GridViewColumn Header="Kota" Width="118" DisplayMemberBinding="{Binding kota}" />
                                <GridViewColumn Header="Area" Width="70" DisplayMemberBinding="{Binding area}" />
                                <GridViewColumn Header="Tipe" Width="100" DisplayMemberBinding="{Binding nm_tipe}" />
                                <GridViewColumn Header="Nama PIC" Width="139" DisplayMemberBinding="{Binding nm_pic}" />
                                <GridViewColumn Header="Tlp PIC" Width="139" DisplayMemberBinding="{Binding tlp_pic}" />
                                <GridViewColumn Header="Action" Width="79">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <fa:ImageAwesome Icon="Edit" Height="12" Width="12" Margin="0 0 20 0" Foreground="Black"/>
                                                <fa:ImageAwesome Icon="Trash" Height="12" Width="12" Margin="0 0 0 0" Foreground="Black"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
